/* Crie um programa que lê 6 valores inteiros e, em seguida, mostre na tela os valores lidos */
#include <stdio.h>

int main() {
    int i, Vetor[6] = {14, 2, 4, 7, 8, 1};
    for (i = 0; i < 6; i++) {
        printf("%d\n", Vetor[i]);
    }
    return 0;
}
